import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginHorizontal: 20,
    // marginTop: 100,
  },
  topImageContainer: {
    flexDirection: 'row',
    paddingTop: 10,
  },
  topImage: {
    height: 80,
    width: 90,
    borderColor: 'grey',
    borderWidth: 1,
    borderRadius: 5,
  },
});

export default styles;
